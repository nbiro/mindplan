---
id: i-init-project
type: Interaction
title: "Init (sources migration)"
description: "Init migrates implementation_packages to sources; drops --layout; seeds implements and roles."
state: stable
shipped_at: 2026-09-23T14:05:44.676Z
created_at: 2026-07-19T18:33:36.816Z
updated_at: 2026-09-23T14:08:09.274Z
belongs_to:
  - j-agent-onboarding
depends_on:
  - f-territory-store
  - f-source-index
leads_to:
  - i-orient-plan
implements:
  - src/interactions/i-init-project/
---


# Init (sources migration)

## Purpose

Scaffold consumer repos and migrate brownfield configs. One-time migration from `implementation_packages` to `{ sources, exclude }`, seed `implements` and Foundation `role`. Drop `--layout`.

## Actor & Trigger

- **Actor:** maintainer or agent via CLI (`if-cli`)
- **Trigger:** `mindplan-mcp init` (`-f` / `--force` only; no `--layout`)

## Execution Logic

Intended files: `src/interactions/i-init-project/`

- Keep installers + `resolvePackageRoot` + `runInit`
- Migration when config still has `implementation_packages`:
  - `required` → `sources: ["src/**"]`; seed each node's implements (live + open next) from `src/<kind>/<id>/` skipping `.gitkeep`
  - `off` → `sources: []`, empty implements
  - Both: fill Foundation `role` from description prefix (Assembler/Infra/Design system/Adapter), default `infra`
  - Server-side write; shipped nodes do not need open_next for seed
- Drop `--layout` / `forceLayout` from `runInit` and install helpers
- Write `{ sources, exclude }` config shape owned by `f-source-index`

Depends on: `f-territory-store`, `f-source-index`.

## Acceptance Criteria

- Init migrates old configs and seeds implements/role
- No `--layout` flag remains
- `-f` still force-overwrites agent assets
- Verified via `npm test` / smoke

## Atomic Ops

- [x] Implement implementation_packages → sources migration + seed
- [x] Remove `--layout` from runInit and CLI contract
- [x] Keep force install behavior
- [x] Verified via `npm test`

## Attachments

_None._
