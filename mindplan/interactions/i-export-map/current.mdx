---
id: i-export-map
type: Interaction
title: "See the map"
description: "Owns export_mindplan_view MCP handler and the view export used by the CLI view/export command."
state: stable
shipped_at: 2026-09-23T13:27:24.596Z
created_at: 2026-07-19T18:33:36.804Z
updated_at: 2026-09-23T13:27:24.596Z
belongs_to:
  - j-territory-sdlc
depends_on:
  - f-territory-store
  - f-view-projection
  - f-compiler-rules
---



# See the map

## Purpose

Export a deterministic Mermaid or DOT projection of the territory graph for PRs and docs. Also provides the view-export entry used by the CLI `view` / `export` commands.

## Actor & Trigger

- **Actor:** coding agent via MCP (`if-mcp-tools`) or human/CI via CLI (`if-cli`)
- **Trigger:** `export_mindplan_view` tool or `mindplan-mcp view|export`

## Execution Logic

Package: `src/interactions/i-export-map/`

- Handler for `export_mindplan_view` (registered by `if-mcp-tools`)
- Export function callable by `if-cli` for `view` / `export` (format, focus, include_retired, output file)
- Projection substrate: `f-view-projection`
- Optional focus validation via `f-compiler-rules` `findNode`
- Automatic `mindplan/map.md` refresh after mutations is owned by `i-steer-plan` (calls `f-view-projection` directly)

Depends on: `f-territory-store`, `f-view-projection`, `f-compiler-rules`. Belongs to: `j-territory-sdlc`.

## Acceptance Criteria

- `export_mindplan_view` returns Mermaid and DOT (full or focused)
- CLI `view` / `export` produce the same diagram output as before
- Handler lives under this package; Interfaces only wire it
- `npm test` passes

## Atomic Ops

- [x] Move `export_mindplan_view` handler + CLI-callable export into this package
- [x] Export functions for `if-mcp-tools` and `if-cli`
- [x] Verify MCP export + CLI view via smoke/tests

## Attachments

_None._
