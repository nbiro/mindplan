---
id: wf-npm-publish
type: Workflow
title: "Publish to npm"
description: "Ship mindplan-mcp@0.1.0 to the public npm registry via a documented manual first publish, then update consumer install docs."
state: draft
created_at: 2026-07-15T18:51:52.964Z
updated_at: 2026-07-19T19:40:00.111Z
belongs_to:
  - j-agent-onboarding
depends_on:
  - f-mcp-runtime
---

# Publish to npm

## Purpose

Ship `mindplan-mcp` to the public npm registry so consumers can install without cloning and building from source. This Workflow is still **draft** — the runbook below is the intended full contract for first publish and docs sync.

## Goal

A maintainer can publish the package once, then any consumer can:

```bash
npm install -g mindplan-mcp
# or
npx mindplan-mcp init
```

and get a working CLI + MCP server whose `templates/` and `dist/` match this repo’s contract.

## Acceptance Criteria

- [ ] Package name `mindplan-mcp` is claimed on the public registry at version `0.1.0` (or the next SemVer if `0.1.0` was already taken by us in a prior attempt).
- [ ] `npx mindplan-mcp --help` (or `mindplan-mcp help` after global install) shows init/view/help usage.
- [ ] Published tarball includes `dist/`, `templates/`, `README.md`, `SPEC.md`, and `LICENSE` — not `src/`, tests, or local `mindplan/` territory.
- [ ] README Quick start no longer leads with “install from source” as the only path; npm install / `npx` is primary, source build remains as a contributor path.
- [ ] `CONTRIBUTING.md` Publishing section matches the runbook below (manual first; CI/OIDC explicitly deferred).

## Versioning policy

- **Scheme:** SemVer starting at current `package.json` version **`0.1.0`**.
- **Until 1.0.0:** treat the public API (MCP tools, CLI, SPEC behaviour) as evolving; prefer `minor` for additive tool/CLI changes, `patch` for fixes, `major` only for breaking tool/contract changes that land while still on 0.x if we choose to signal breakage that way — otherwise document breakage in changelog and SPEC.
- **First publish:** publish whatever is currently in `package.json` (expected `0.1.0`) — do not bump solely to “make publishing feel real.”
- **Later releases:** `npm version patch|minor|major` then `npm publish`; tag the git commit when convenient, but **tag-driven CI is out of scope for this Workflow**.

## Release style (this Workflow)

**Manual first publish.** Maintainer machine + npm account with publish rights to `mindplan-mcp`.

Out of scope for this node (follow-up Workflow / version):

- GitHub Actions release job
- npm Trusted Publishing (OIDC)
- Automated changelog generation

## Execution Logic

Package: `src/workflows/wf-npm-publish/` (pointer until publish automation exists). Depends on: `f-mcp-runtime`. Belongs to: `j-agent-onboarding`.

### Preflight

1. Confirm registry identity: npm org/user that should own `mindplan-mcp` (align with github.com/nbiro/mindplan).
2. Confirm `package.json` metadata is release-ready (`name`, `version`, `bin`, `files`, `engines`, repository URLs).
3. Dry-run the tarball: `npm pack --dry-run` / `npm publish --dry-run` — no secrets, no `.cursor/`, no local `mindplan/` territory, no `src/`.
4. `npm test` green on the release commit.

### Publish

5. `npm login` (2FA as required).
6. Clean tree on release commit: `npm publish` (`prepublishOnly` runs build).
7. Fresh-install verify: `mindplan-mcp help` + `mindplan-mcp init` in a temp directory.

### Docs sync

8. README Quick start: npm / `npx` primary; clone+build for contributors.
9. `CONTRIBUTING.md` Publishing matches this runbook (manual first; CI deferred).

## Failure / recovery

- **Name taken by someone else:** stop; escalate — do not rename without a product decision.
- **`0.1.0` already published by us:** bump patch/minor and publish; update docs.
- **Broken publish after install check:** ship a patch; never leave README advertising a broken path.

## Checklist

- [ ] Package metadata + `npm pack` / publish dry-run reviewed
- [ ] `npm test` green on the release commit
- [ ] `mindplan-mcp@0.1.0` published to the public registry
- [ ] Fresh-install verification (`help` + `init`) passed
- [ ] README Quick start updated for npm / `npx` primary install
- [ ] `CONTRIBUTING.md` Publishing section matches this runbook

## Attachments

Optional later: registry screenshots or dry-run logs under `attachments/`.
