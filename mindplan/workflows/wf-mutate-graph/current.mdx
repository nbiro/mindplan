---
id: wf-mutate-graph
type: Workflow
title: "Mutate graph"
description: "Mutate graph; update_node_status accepts cancelled for pre-ship abandon"
state: stable
shipped_at: 2026-07-20T20:11:40.803Z
created_at: 2026-07-15T18:51:48.233Z
updated_at: 2026-07-23T20:59:42.453Z
belongs_to:
  - j-territory-sdlc
depends_on:
  - f-mcp-runtime
  - f-compiler-rules
---




# Mutate graph

## Purpose

Write-path MindPlan tools: create/link nodes, advance pipeline state, optional territory patches, open/discard/promote evolutions, and mistaken-ship recovery via `force_unship` — always through the compiler. Successful graph mutations return `changed_files` (repo-relative paths written or deleted) so hosts can surface what MCP touched.

## Execution Logic

Package: `src/workflows/wf-mutate-graph/`

- `tools.ts` — ownership list `MUTATE_TOOLS` registered by `f-mcp-runtime/server.ts`:
  - `create_node`, `link_nodes`, `unlink_nodes`
  - `update_node_status` (current or `next` slot; `ship` promotes `next`)
  - `force_unship` (user-confirmed recovery: clear `shipped_at`, set pre-ship state)
  - `patch_node_territory` (optional fallback for body/title/description/checkbox toggles; defaults to `next` when evolving)
  - `open_next`, `discard_next`

Interactive agents prefer host file tools for territory prose (`title` / `description` / body / checkboxes) at `current_path` / `next_path`; MCP remains mandatory for graph structure and server-owned frontmatter. Graph mutate responses include `changed_files`. `patch_node_territory` remains for automation and hosts with weak file tools.

`force_unship` requires an explicit human yes and `confirm: "unship:<node_id>"` — agents must not invent confirmation.

Compiler gates live in `f-compiler-rules`. Depends on: `f-mcp-runtime`, `f-compiler-rules`. Belongs to: `j-territory-sdlc`.

## Acceptance Criteria

- All eight mutate tools are registered and persist through the territory store
- Illegal mutations return `Blocked:` and leave the graph unchanged
- Successful graph mutations refresh `mindplan/map.md` via the runtime
- Successful graph mutations (`create_node`, `link_nodes`, `unlink_nodes`, `update_node_status`, `force_unship`, `open_next`, `discard_next`) return `changed_files` listing territory paths, optional `src/` scaffolds, and `mindplan/map.md` when refreshed
- `patch_node_territory` remains usable and returns `changed_files` / `path` for the slot written
- Verified via `npm test`

## Atomic Ops

- [x] Surface `cancelled` via `update_node_status` (compiler-backed)
- [x] Document pre-ship cancel in territory / playbook
- [x] Smoke coverage for cancel gates (dependents, terminal, hide from view)
- [x] Keep mutate tool list and `changed_files` contract intact

## Attachments

_Wireframes and notes go in `attachments/` / `next-attachments/`._
