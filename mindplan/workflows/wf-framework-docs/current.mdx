---
id: wf-framework-docs
type: Workflow
title: "Framework documentation"
description: "Own MindPlan normative docs: SPEC, playbooks, skills (define-entities + plan-project), README — Foundation roles, Territory Completeness, plan-only sessions, and agent process guidance."
state: stable
shipped_at: 2026-07-19T19:58:36.178Z
created_at: 2026-07-19T18:33:36.834Z
updated_at: 2026-07-19T19:58:36.178Z
belongs_to:
  - j-territory-sdlc
  - j-agent-onboarding
depends_on:
  - f-domain-model
---




# Framework documentation

## Purpose

Own MindPlan’s normative documentation surfaces so agents and humans share one process: taxonomy, compiler gates, Territory Completeness, Foundation roles, plan-only planning sessions, and how to evolve shipped work without destroying the living contract.

## Execution Logic

Docs live at repo root and under `templates/agent/` / `mindplan/agent/`. The package folder `src/workflows/wf-framework-docs/` is a pointer (no runtime TS).

Surfaces owned by this Workflow:

- `SPEC.md` — normative language (taxonomy, versioning, Territory Completeness, compiler rules)
- `AGENTS.md` — always-applied agent playbook (repo root)
- `templates/agent/playbook.md` — consumer playbook shipped by `mindplan-mcp init`
- `mindplan/agent/playbook.md` — local mirror of the playbook
- `templates/agent/skills/define-entities/` (+ mindplan/agent mirror) — entity scaffolding skill
- `templates/agent/skills/plan-project/` (+ mindplan/agent mirror) — plan-only sessions (no application code)
- `README.md` — product framing and taxonomy summary

Foundation scaffold copy that mentions roles lives in `src/foundations/f-territory-store/store.ts` (touched when role/description conventions change).

### Territory Completeness

`current.mdx` is the complete contract of what a node *is* in the repository now. `next.mdx` is a complete proposed successor of that same contract (seeded from current, edited in place). Atomic Ops on `next` may be evolution-scoped; PRD/spec sections must remain a full post-ship document. Delta-only or changelog-only `next` bodies are forbidden because `ship` promotes `next` over `current`.

### Foundation roles (docs convention)

Assembler, Infra, Design system, Adapter — documentation groupings only (not NodeTypes). Agents SHOULD tag the role at the start of a Foundation `description`.

### Plan-only sessions

When the user wants to model or restructure the product plan without writing application code, agents follow `mindplan/agent/skills/plan-project/`. The always-on playbook routes those sessions to that skill and forbids advancing into implementation (`in-progress` / `src/`) until a later execution session.

## Acceptance Criteria

- SPEC documents Territory Completeness and Foundation roles.
- Playbooks (AGENTS + templates + mindplan mirror) instruct agents to keep `current`/`next` as full contracts and forbid delta-only promotion.
- Playbooks route plan-only / architecture-modeling asks to the `plan-project` skill.
- define-entities skill aligns with Foundation role tagging.
- plan-project skill forbids application-code writes and pipeline states past `ready`.
- README summarizes Foundation roles and mentions both agent skills.
- Active MindPlan Journey/Foundation/Workflow territories describe full repo state (not stubs or latest-change deltas).

## Atomic Ops

- [x] Author `templates/agent/skills/plan-project/SKILL.md` (plan-only loop; hard no-code rules)
- [x] Update playbook (templates + AGENTS.md + mindplan/agent mirror) to route plan-only work to plan-project
- [x] Mirror plan-project skill under `mindplan/agent/skills/plan-project/`
- [x] Point README / integrations shared assets at both skills
- [x] This next.mdx remains a full successor contract (not a changelog)

## Attachments

_Wireframes and notes go in `attachments/` / `next-attachments/`._
