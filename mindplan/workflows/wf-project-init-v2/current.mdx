---
id: wf-project-init-v2
type: Workflow
title: "Project init (blast-radius orient)"
description: "Scaffold consumer mindplan/ plus agent playbook requiring get_blast_radius before Foundation/Workflow implementation; define-entities skill, MCP example, integrations, and AGENTS.md."
state: stable
shipped_at: 2026-07-15T20:12:38.451Z
created_at: 2026-07-15T20:09:06.542Z
updated_at: 2026-07-15T20:12:38.451Z
belongs_to:
  - j-agent-onboarding
depends_on:
  - f-territory-store
---

# Project init (blast-radius orient)

Scaffold consumer mindplan/ plus agent playbook requiring `get_blast_radius` before Foundation/Workflow implementation; define-entities skill, MCP example, integrations, and AGENTS.md.

## Execution Logic

1. Run `node dist/index.js init` (or `mindplan-mcp init`) from project root.
2. Creates `mindplan/` type dirs and installs from `templates/agent/`:
   - playbook (with blast-radius orient step), define-entities skill, mcp.json.example, integrations guides
3. Creates root `AGENTS.md` only when missing.

Playbook change: agents must call `get_blast_radius` on the owning Foundation/Workflow node before substantial implementation — in request routing, build-pipeline Orient, MCP tools table, and Never do.

## Checklist

- [x] Requirements defined (playbook must mandate blast radius before implementation)
- [x] Implementation complete (`AGENTS.md`, `templates/agent/playbook.md`, `mindplan/agent/playbook.md`)
- [x] Tests passing (smoke covers init; no playbook-specific test)

## Attachments

_Wireframes, screenshots, and spec PDFs go in `attachments/`._

{/* This file is MDX. MindPlan standard components (AtomicOp, AcceptanceCriteria, Attachment, StateBadge, DependsOn, BelongsTo) may be used here; see SPEC.md §6.4. */}
