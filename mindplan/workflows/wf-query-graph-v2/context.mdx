---
id: wf-query-graph-v2
type: Workflow
title: "Query graph (blast radius via supersedes)"
description: "get_blast_radius seeds from the supersedes chain so agents see what the predecessor still serves while building a successor version."
state: stable
shipped_at: 2026-07-15T19:59:26.977Z
created_at: 2026-07-15T19:58:14.741Z
updated_at: 2026-07-15T19:59:26.977Z
belongs_to:
  - j-territory-sdlc
depends_on:
  - f-mcp-runtime
supersedes:
  - wf-query-graph
---

# Query graph (blast radius via supersedes)

Read orientation tools: find_related_nodes, get_mindplan_graph, get_node_context, and get_blast_radius — with blast radius aware of version lineage.

## Execution Logic

1. Agent calls `find_related_nodes` with a query (preferred orientation).
2. Optionally `get_node_context` for full territory body/attachments.
3. `get_blast_radius` for transitive dependents before risky changes. When the focus node has a `supersedes` chain, seed the reverse-`depends_on` BFS from the focus **and** each predecessor at distance 0; return `via_supersedes` listing those predecessor ids so the agent understands why affected nodes appear.
4. `get_mindplan_graph` only for greenfield/full audits.

Ranking lives in `src/search.ts`; blast radius and assembly via `src/rules.ts` / store `loadGraph`.

## Checklist

- [x] SPEC: `get_blast_radius` documents supersedes seeding + `via_supersedes`
- [x] Implement supersedes-chain seed in `transitiveDependents` / blast handler
- [x] Smoke: blast radius of a draft successor includes predecessor dependents
- [x] Playbook / README wording updated

## Attachments

_Wireframes and notes go in `attachments/`._
