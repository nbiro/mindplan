---
id: f-github-actions
type: Foundation
title: "GitHub Actions assembler"
description: "Assembler — GitHub Actions mounting CI and release workflows for the mindplan-mcp repo."
state: stable
shipped_at: 2026-07-20T20:11:40.696Z
created_at: 2026-07-19T20:19:02.547Z
updated_at: 2026-07-20T20:11:40.696Z
depends_on:
  - f-npm-registry
---

# GitHub Actions assembler

## Purpose

Assembler that mounts CI and release jobs for this repository on GitHub Actions. Owns the **CI substrate** and **OIDC permission conventions** for any job that publishes. Does **not** own the release interaction YAML body — that is `i-npm-tag-publish`.

## Ownership split

| Surface | Owner |
|---------|--------|
| `.github/interactions/ci.yml` (test CI) | **This Foundation** |
| OIDC / `registry-url` / `id-token: write` conventions for publish jobs | **This Foundation** (applies `f-npm-registry` contract) |
| `.github/interactions/publish.yml` (tag-driven release job steps) | **`i-npm-tag-publish` only** |

Do not duplicate Atomic Ops for writing `publish.yml` on this Foundation.

## Shared Substrate Spec

### Existing CI

- File: `.github/interactions/ci.yml`
- Triggers: push/PR to `main` / `master`
- Matrix: Node 18, 20, 22 — `npm ci` → `npm run build` → `npm test` → integrity checks
- PR: `node dist/index.js check --base <PR base sha>` (dirty-src + packages)
- Every run: `node dist/index.js check --for-main` (no mid-pipeline states)
- Branch protection SHOULD require the `--for-main` job and disallow direct pushes to `main`
- Release automation MUST NOT weaken this gate

### Conventions for publish jobs (consumed by `i-npm-tag-publish`)

When `i-npm-tag-publish` authors `.github/interactions/publish.yml`, it MUST follow:

- Permissions: `contents: read`, `id-token: write` (per `f-npm-registry`)
- `actions/setup-node` with `registry-url: https://registry.npmjs.org`
- Node: pin one LTS aligned with CI (prefer 22 or 20); no matrix publish
- Invoke the shared publish script from `i-npm-publish` in CI mode (that mode **must** run `npm test` — do not drop tests for a bare `npm publish`)

### Depends on

- `f-npm-registry` — OIDC / Trusted Publishing **contract** (docs); live npm binding is still `i-npm-tag-publish`

### Out of scope

- Authoring or owning `publish.yml` job steps (Interaction `i-npm-tag-publish`)
- Publish script contents (Interaction `i-npm-publish`)
- Changelog automation

## Implementation

Package: `src/foundations/f-github-actions/`. CI YAML lives under `.github/interactions/ci.yml` as the deployable surface this Assembler owns. Release YAML is written by `i-npm-tag-publish` under `.github/interactions/publish.yml` (repo-root interaction path is an intentional exception to prescribed packages, same as CI).

## Acceptance Criteria

- CI interaction remains green on PRs after any assembler changes
- OIDC + `registry-url` conventions for publish jobs are documented for Interaction consumers
- Ownership of `publish.yml` is clearly assigned to `i-npm-tag-publish` only

## Checklist

- [x] Document CI substrate (`.github/interactions/ci.yml`) as owned by this Foundation
- [x] Wire `mindplan-mcp check` / `--for-main` into CI (build-from-source)
- [x] Document OIDC / `registry-url` / `id-token: write` conventions for publish jobs
- [x] Document ownership split: `publish.yml` owned solely by `i-npm-tag-publish`
- [x] Document that CI-mode publish must run tests via the shared script (no bare publish)
- [x] Document branch protection: require PR + `--for-main` status; no direct push to main

## Attachments

Optional: interaction diagrams under `attachments/`.
